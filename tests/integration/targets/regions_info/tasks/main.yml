---
- block:

    - name: Ensure API key is provided
      ansible.builtin.fail:
        msg: do_api_key should be defined in tests/integration/integration_config.yml
      when:
        - do_api_key is not defined
        - do_api_key | length == 0

    - name: Get regions
      community.digitalocean.regions_info:
        token: "{{ do_api_key }}"
      register: result

    - name: Verify regions
      ansible.builtin.assert:
        that:
          - not result.changed
          - result.msg is string
          - result.msg == "Current regions"
          - result.regions is defined
          - result.regions.links is defined
          - result.regions.meta is defined
          - result.regions.regions is defined
