---
- block:

    - name: Ensure API key is provided
      ansible.builtin.fail:
        msg: do_api_key should be defined in tests/integration/integration_config.yml
      when:
        - do_api_key is not defined
        - do_api_key | length == 0

    - name: Get DigitalOcean account information
      community.digitalocean.account_info:
        token: "{{ do_api_key }}"
      register: result

    - name: Verify account information idempotency and formatting
      ansible.builtin.assert:
        that:
          - not result.changed
          - result.data is defined
          - result.data.account is defined
          - result.data.account.droplet_limit is defined
          - result.data.account.email is defined
          - result.data.account.email_verified is defined
          - result.data.account.floating_ip_limit is defined
          - result.data.account.reserved_ip_limit is defined
          - result.data.account.status is defined
          - result.data.account.status_message is defined
          - result.data.account.team is defined
          - result.data.account.team.name is defined
          - result.data.account.team.uuid is defined
          - result.data.account.uuid is defined
          - result.data.account.volume_limit is defined
