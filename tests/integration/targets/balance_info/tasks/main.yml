---
- block:

    - name: Ensure API key is provided
      ansible.builtin.fail:
        msg: do_api_key should be defined in tests/integration/integration_config.yml
      when:
        - do_api_key is not defined
        - do_api_key | length == 0

    - name: Get balance information
      community.digitalocean.balance_info:
        token: "{{ do_api_key }}"
      register: result

    - name: Verify balance information
      ansible.builtin.assert:
        that:
          - not result.changed
          - result.msg is string
          - result.msg == "Current balance information"
          - result.balance is defined
          - result.balance.account_balance is defined
          - result.balance.generated_at is defined
          - result.balance.month_to_date_balance is defined
          - result.balance.month_to_date_usage is defined
